#include <stdio.h>
int main(void) {
    int t,a,b,c;
    printf("Enter integer: Enter integer: Enter integer: ");
    scanf("%d %d %d",&a,&b,&c);
    if(a<b)
{
t=a,a=b,b=t;
}
if(a<c)
{
t=a, a=c,c=t;
}
if(b<c)
{
t=b,b=c,c=t;
}
printf("The integers in order are: %d %d %d\n",a,b,c);
return 0;
}
