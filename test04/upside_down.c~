#include <stdio.h>
#include <math.h>
int main(void){
    printf("Please enter three numbers: ");
    double a,b,c;
    scanf("%lf %lf %lf",&a,&b,&c);
    if(a>b&&b>c){
    printf("down");
    }
    else if(a<b&&b<c){
    printf("up");
    }
    else {
    printf("neither");
    }
    printf("\n");
    return 0;
}
